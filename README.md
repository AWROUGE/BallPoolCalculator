# BallPool25

##Type in values e.g (radius, depth/width/height and number of balls) and leave out the desired value in blank, then then click "Calculate" and the value will fill itself in the box.<br>
if you wish to find other values with the current information you can simply delete the old values and the program will fill in the new answer...
